    <div class="row">
        <div class="col-md-2 offset-2">
            <a href="{{ path('create_quiz') }}" class="btn btn-success">Utwórz nowy quiz</a>
        </div>
    </div>
{% if quizzes is not null %}
    {% for quiz in quizzes %}
        <a href="{{path('quiz', {id: quiz.id})}}">
            <div class="row">
                <div class="col-md-8 offset-2">
                    <div class="tm-flex"  style="min-height: 200px">
                        <div class="tm-bg-white-translucent text-xs-left tm-textbox tm-textbox-padding tm-white-box-margin-b">
                            <h2 style='position: relative' class="tm-text-title">{{ quiz.name }}
                                {% if app.user.role == 'ROLE_USER' %}
                                {% if quiz.status == 1 %}
                                <span style="position:absolute;right: -75px;font-size:135px">&raquo;</span></h2>
                            <p class="tm-text">{{ quiz.description }}</p>
                            {% else %}
                                <p class="tm-text">Brak dostępu</p>
                            {% endif %}
                            {% else %}
                                <span style="position:absolute;right: -75px;font-size:135px">&raquo;</span></h2>
                                <p class="tm-text">{{ quiz.description }}</p>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </a>
    {% endfor %}
{% endif %}